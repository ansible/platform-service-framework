_subdirectory: "{{template}}"

template:
  choices:
    - templates/project
    - templates/app

project_name:
  type: str

src_branch:
  type: str
  default: "main"

app_name:
  type: str
  default: ""

apps:
  default:
    - api